{% trans_default_domain 'FOSUserBundle' %}

<div class="row">
	<div class="col-md-6">
		<h3>Recovery Password</h3>
		<br>
		<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="form-horizontal fos_user_resetting_request" >
			<fieldset style="border: none;">
			 <div class="form-group">
		        {% if invalid_username is defined %}
		            <p>{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}) }}</p>
		        {% endif %}
		        <label for="username" class="col-sm-4 control-label username">{{ 'resetting.request.username'|trans }}</label>
		        <div class="col-sm-8">
		 	       <input type="text" id="username" name="username" required="required" class="form-control"/>
		        </div>
		    </div>
		    </fieldset>
			<div class="form-actions clearfix">
			    <button type="submit" class="btn btn-primary btn-lg" style="float: right;" name="reestablecer">{{ 'resetting.request.submit'|trans }}</button>
			</div>
		</form>
	</div>
</div>


